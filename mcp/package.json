{
  "name": "@eos-ai/mcp-server",
  "version": "2.3.0",
  "description": "EOS MCP server — git-native code context (ownership, reviewers, blast radius, activity, contributors) over your git index.",
  "mcpName": "io.github.eos-xyz/eos-context",
  "license": "MIT",
  "private": false,
  "bin": {
    "eos-mcp": "./dist/bin/eos-mcp.js"
  },
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "typecheck": "NODE_OPTIONS='--max-old-space-size=8192' npx tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.5.1",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
